terraform {
  required_version = ">= 1.5.0"

  backend "s3" {}

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.40"
    }
  }
}

provider "aws" {
  profile = trimspace(var.aws_profile) != "" ? var.aws_profile : null
  region  = var.region
}